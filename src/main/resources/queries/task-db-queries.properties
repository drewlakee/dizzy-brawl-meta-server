select-all-tasks-by-account-id=SELECT * FROM task WHERE account_id = $1
delete-task-by-task-uuid=DELETE FROM task WHERE task_uuid = $1
insert-task-by-account-uuid=INSERT INTO task (task_uuid, active_interval, current_state, generated_date, goal_state, task_type_id, account_id) VALUES ($1, $2, $3, $4, $5, $6, $7)
update-task-progress-by-task-uuid=UPDATE task SET current_state = $1 WHERE task_uuid = $2
